job:
    only:
        - tags
        - triggers
    tags:
        - master
    script:
        - gitbook install
        - gitbook pdf . media/gitbook.pdf
        - gitbook build
        - syncgitbook

